---
const PROJECTS =[
    {
      title: "proyecto 1",
      description: "alkdfaskdf",
      link: "#",
      github:"#",
      image: "placehorlder",
      tags: ["HTML", "CSS","Javascript" ],

    }
]
---

{
    PROJECTS.map(({ title, description, link, github, image, tags})=> (
        <article>
        <h3>{title}</h3>
        <p> {description}</p>
        <ul>
            {tags.map(tag =>(
                <li>{tag}</li>
            ) ) }
        </ul>
        <img src={image} alt={`Captura de pantalla del proyecto ${title}`} />
        </article>
    ) )
}